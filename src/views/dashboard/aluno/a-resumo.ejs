<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/resumo.css">
    <link rel="stylesheet" href="/css/header2.css">
    <link rel="shortcut icon" href="../../Images/favicon.ico" type="image/x-icon">
    <title>Resumos</title>
</head>
<body>
    <header>
        <a href="/aluno/a-minharotina"><img src="/images/voltar.svg" alt=""></a>
        <p>MÉTODOS DE ESTUDO</p>
        <div>
          <p><%= user.name %></p>
          <button class="user-icon" id="userIcon" aria-label="Abrir menu do usuário">
              <img  src="/images/perfil.svg" alt="imagem do perfil">
              <div class="user-config" id="userConfig">
                  <ul>
                      <li><a href="/aluno/a-perfil">MENU</a></li>
                      <li><a href="/aluno/a-config">CONFIGURAÇÕES</a></li>
                      <li><a href="/auth/logout">SAIR</a></li>
                  </ul>
              </div>
          </button>
      </div>
      </header>
    <h1><%= title %></h1>
    <main>
      <form class="container" action="<%= resumo ? '/aluno/resumo/update/' + resumo.id : '/aluno/resumo/save' %>" method="POST">
        <% if (resumo) { %><input type="hidden" name="id" value="<%= resumo.id %>"><% } %>
        <div class="divisor">
        <section class="controlador">
          <section class="info-resumo">
            <h2 class="subtitle">TÍTULO</h2>
            <input type="text" name="titulo" class="descricao" placeholder="Escreva o título aqui" value="<%= resumo?resumo.titulo:'' %>" required>
            <h2 class="subtitle">CATEGORIA</h2>
            <select name="categoria" class="descricao" required>
              <option value="">Selecione</option>
              <option value="Biologia" <%= resumo&&resumo.categoria==='Biologia'?'selected':'' %>>Biologia</option>
              <option value="História" <%= resumo&&resumo.categoria==='História'?'selected':'' %>>História</option>
              <option value="Outros" <%= resumo&&resumo.categoria==='Outros'?'selected':'' %>>Outros</option>
            </select>
          </section>
        </section>
        <section class="caixa-de-texto">
          <h2 class="subtitle">RESUMO</h2>
          <textarea name="conteudo" rows="15" class="mensagem" placeholder="Escreva o resumo aqui..." required><%= resumo?resumo.conteudo:'' %></textarea>
        </section>
      </div>
        <section>
          <button type="submit" class="save-button">SALVAR <img src="/images/salvar.svg"></button>
        </section>
      </form>
     </main>
    <script src="/js/icon-menu.js"></script>  
</body>
</html>